# PROJECT_PRINCIPLES.md — 開発憲章

**本書は、このプロジェクトに参加する全てのAI・開発者が最初に読む「プロジェクト憲法」である。**
設計書ではない。「このプロジェクトは何を目指し、何を絶対に守るか」を定める。

## 第0条 最上位原則(本書の地位)

本書は、本プロジェクトにおける**最上位の設計思想**である。

- 設計書・仕様書・ソースコード・AIによる提案が本書と矛盾した場合は、**必ず本書を優先する**
- **AIは本書を無断で変更してはならない**
- 思想の変更は**プロジェクトオーナーの判断**による。**オーナーの明示的な確認を得た場合は、AIが本書を直接編集してよい**(その確認の事実と差分を、決定ログまたはコミットに必ず残す)

> **2026-08-02 改訂(第0条)**: 「AIは変更してはならない(適用はオーナー自身の手で)」から
> 「オーナーの確認を得ればAIが編集できる」へ改めた。本改訂は所有者本人の判断による(本人の発話を確認のうえAIが適用)。

---

## 第1条 プロジェクト理念

このアプリは、**占術を設計図として競馬を予想する**アプリである。

星は運勢ではなく設計図であり、設計図は地(環境)と時間(天)に触れて、その日ごとの表れ方を変える。
本アプリが読むのは「**今日、どの馬が最も設計図を活かせる日なのか**」である。

**予想が的中することを、本アプリは目的として掲げる。** 利用者が馬券を買い、その馬を応援し、
予想が花開く瞬間を共に味わうこと。それがこのアプリの最大の喜びである。

### 分析対象と利用者の位置づけ

本アプリでは、**利用者自身もレースという出来事の参加者である**と考える。分析する対象は次の4者である。

- 競走馬
- 騎手
- レース日
- 利用者

競走馬だけではなく、利用者自身との占術的な関係性も分析することで、**その利用者だけの視点による鑑定結果**を提供する。

ただし、**結果を保証はしない**。占術に基づく分析であり、外れることがある。
断定表現の禁止(第8条)と、仮説ステータスの明示(第4条)は、目的が変わっても維持する。

> **2026-08-02 改訂(第1条)**: 「当てることが目的ではない」を改め、予想の的中を目的として掲げた。
> 理由は、利用者は馬券を買うから応援するのであって、買わない者はそもそも競馬を見ない、という観測に基づく。
> 本改訂は所有者本人の判断による。
>
> **2026-08-02 追記(製品の形)**: 本アプリ自体を他人に見せる・使わせることは前提としない。
> アプリはオーナー自身の道具であり、**他者へ渡す商品は「鑑定書」(および予想・検証の発信)**である。

## 第2条 二正本の原則(Single Source of Truth)

| 正本 | 場所 | 内容 |
|---|---|---|
| **知識ベースの唯一の正本** | `KomaVault/05_Knowledge/占術知識ベース_v1.3/`(現行版・17ファイル。2026-08-02発行。v1.2以前は凍結) | 占術の知識・意味・相性ルール・年月日運・鑑定文テンプレート・禁止語・出典・仮説ステータス・schema.sql |
| **ソースコードの唯一の正本** | GitHub(`keiba-fortune-engine`) | 実装・テスト・ドキュメント・本憲章 |

- **知識をコードへ書いてはいけない。** エンジンはJSON・SQL・仕様書を読み込んで構築する
- 知識ベースを変更してもコードを書き換えずに済む設計を維持する
- 起動時に正本の**全ファイル**(件数と一覧は`正本参照.md`のハッシュ表が定める。現行v1.3=17ファイル)をサイズ+SHA-256で検証する。不一致は起動失敗またはdegraded。**黙ってフォールバックしない**
- 旧版スナップショット・作業フォルダ内の旧JSONを正本として参照しない

> **2026-08-02 改訂(第2条)**: 知識ベースの正本をGoogle DriveからKomaVaultへ移した。Googleドライブから撤退したため。
> 旧正本 `黄道十二宮、西洋占星術、数秘術/db` は**凍結**し、以後参照しない(内容は新正本とバイト単位で同一であることを確認済み)。
> 参照先を変更した箇所: `backend/app/core/config.py`(kb_dir) / `docker-compose.yml`(volume) / `.env.example` / `docs/KnowledgeBase.md`。
> **本改訂は所有者本人の判断による。**
>
> **2026-08-02 改訂(第2条・2回目)**: 知識ベースをv1.3(17ファイル)へ版上げ(oshi_v0 / sizhu_day_pillar /
> wuxing_correspondences / racecourse_geography を収録。synchro_v0は重み矛盾のため見送り)。
> ファイル数の固定表記をやめ、`正本参照.md` のハッシュ表を数の正とした。本改訂は所有者本人の判断による(確認のうえAIが適用)。

## 第3条 AIの役割

**AIは占術家ではない。占術計算は知識ベースが決定する。**

AIが担当するのは次の範囲である。

- 自然な文章化
- 説明
- 比較
- 質疑応答

### 分析で終わらせず、「答え」まで提示する

本アプリは、分析結果を並べるだけでは終わらない。知識ベースに基づく占術結果から、**利用者が参考にできる結論まで提示する**ことを目指す。

ただし、その結論は**占術に基づいて導かれた分析結果**であり、未来やレース結果を保証するものではない。結論の根拠(使用ルール・出典・仮説ステータス)は常に提示し、AIが根拠のない結論を創作してはならない。

AIが勝手に占術ルールを追加してはならない。AI出力も必ず禁止語フィルタを通す。AIを占術計算・スコア決定・相性判定の主体にしない。

### AI開発ルール(コードを書くAIへの規範)

1. 作業開始前に本書と `docs/KnowledgeBase.md` を読むこと
2. 知識・意味・ルール・テンプレート・禁止語・重み・出典・ステータスをコードへハードコードしないこと
3. 知識の不足・矛盾を発見したら、**推測で補完せず**、正本側への修正提案として報告すること(実装は `knowledge_gap` エラーで明示)
4. 未確定の重み・配点を勝手に決定しないこと
5. 新しい占術ルール・解釈・流派を勝手に追加しないこと
6. 秘密情報(APIキー・トークン・パスワード)をコード・コミット・ログへ含めないこと
7. 外部サービスへ無断接続しないこと。スクレイピング禁止
8. 破壊的変更(削除・全面改修)の前に、差分と影響範囲を報告して合意を得ること
9. 検証(テスト・実測)を経ていない実装を「完了」と報告しないこと

## 第4条 開発ルール(仮説と事実の峻別)

- 新しい占術を勝手に追加しない
- **仮説を事実として扱わない**
- 検証されていないものは必ず `validation_required` または `app_hypothesis` として扱い、APIレスポンス・UI・文書にステータスを明示する
- ステータス語彙は正本 `status_vocab.json` に従う。独自ステータスを作らない
- 統計的検証は `verification_plan.json`(事前登録)に従い、交絡(出生月の月齢効果・騎手選択バイアス等)を統制せずに相関を主張しない

## 第5条 知識ベース運用の原則

知識ベースは継続的に成長する。**しかし、新しい占術を増やすことを目的としない。**

最優先するのは次の4点である。

- 既存知識の**検証**
- **品質向上**
- **根拠の明確化**(出典の一次資料化)
- **再現性の向上**

**知識量よりも品質を重視する。** 知識の追加・変更は必ず正本側で行い、版として凍結し(`vX.Y_日付`+MANIFEST)、検証(出典ID・語彙・整合・禁止語)を通してからアプリへ反映する。

## 第6条 品質思想(アーキテクチャ)

- **保守性を最優先**とする
- データ取得元を交換できる設計(DataProvider層)を維持する
- **モジュラーモノリス**を維持し、必要になるまでマイクロサービス化しない
- 過剰設計を避ける。将来の可能性のために今の複雑さを増やさない
- Webアプリ本体にOS依存(Windows依存)コードを含めない。JV-Link等は別ワーカーへ分離する
- データフロー(提供元→Provider変換→共通DTO→エンジン→鑑定文→AI補助→API→UI)を崩さない

## 第7条 競馬データの原則

- **JRA-VANだけに依存しない。** 競馬ブック・NAR・REST API・CSV・法人向けデータ提供など、将来の追加へ対応できること
- 全ての取得元は共通DTOへ変換してから利用する。エンジン・API・UIは取得方式を直接参照しない
- 各データ提供元の規約・ライセンスを遵守する(`horse_data_policy.json` の法務チェックリスト)。スクレイピング禁止
- データ提供元のクレジットは設定値から動的に注入する

## 第8条 エンターテインメントとしての表現

- 「絶対に当たる」「必ず勝つ」等の断定表現は**禁止**。禁止語の正本は知識ベース(`forbidden_expressions`)であり、生成後フィルタを必ず通す
- 馬の心理・体調・結果を事実として断定しない。語尾は傾向表現に統一する
- 全ての鑑定に免責文を付す:「本予想は占術に基づく分析であり、レース結果を保証するものではありません。馬券の購入はご自身の判断と責任で行ってください」
- 「答え」を提示しても、**利用者自身が考える余地を残す。** 結論を押し付けず、判断材料と楽しみを提供する。競馬を楽しむための補助ツールである
- **回収率・的中率は、実測に基づき母数と期間を添えて開示してよい。** 誇大表示・都合のよい期間の切り出しは行わない
- **的中の保証は引き続き禁止**する。「必ず」「絶対」「確実」等の断定は景表法上も許されない
- **ギャンブル依存を助長する表現・設計は引き続き行わない**: 賭け金・資金配分の指示、負けを取り返す文脈、連敗を煽る導線
- 外れた予想を隠さない。**検証結果は良い週も悪い週も同じ形式で開示する**

> **2026-08-02 改訂(第8条)**: 実測に基づく成績開示を解禁し、免責文を差し替えた。保証の禁止・依存への配慮は維持。本改訂は所有者本人の判断による。

## 第9条 GitHub運用ルール

- **ブランチ**: `main` を常にリリース可能な状態に保つ。作業は `feature/*`・`fix/*`・`docs/*` ブランチで行い、Pull Request経由で `main` へマージする。`main` への直接pushは初期構築期を除き行わない
- **Pull Request**: 目的・差分・影響範囲・テスト結果を記載する。本憲章に反する変更はマージしない
- **コミット**: 1コミット=1つの意図。メッセージは日本語可、変更理由を書く
- **秘密情報**: `.env`・鍵・トークンをコミットしない(`.gitignore` で担保し、レビューでも確認する)。誤コミット時は即時失効+履歴からの除去
- **Issue**: 知識ベースへの修正提案は「KB提案」ラベルを付け、コード変更と区別する
- **CI**(導入後): pytest+lintが通らないPRはマージしない
- **リポジトリに知識ベースを含めない**(二正本の分離)

## 第10条 バージョニング

- **知識ベース**: `vX.Y_YYYYMMDD`。更新はKomaVaultの `05_Knowledge/` に新フォルダ(`占術知識ベース_vX.Y`)を新設し、MANIFEST後継を発行する(旧版は凍結・同名ファイル乱立禁止)。アプリ側は `正本参照.md` のハッシュ表更新のみで追随する
- **アプリ**: セマンティックバージョニング(`MAJOR.MINOR.PATCH`)。APIの後方互換を壊す変更はMAJOR
- **鑑定の再現性**: 全ての鑑定結果に使用した知識ベース版(`engine_ver`)を記録する。将来のイングレス表も成果物として版管理する
- 知識ベース更新時は、対応するアプリ版との互換性をREADMEに明記する

## 第11条 品質保証

- **起動時検証**: 正本の全ファイル(基準=`正本参照.md`のハッシュ表)の存在・サイズ・SHA-256・JSON構文・必須キー・出典ID参照・ステータス語彙・鏡写し整合(day_theme)を検証する
- **テスト必須**: 占術計算は決定的であり、既知の入出力でユニットテストする。改ざん検知(degraded遷移)・禁止語フィルタ・API通しをテストで担保する
- **禁止語ゼロ**: リリース前に生成文の禁止語ゼロを実測で確認する
- **エラー設計**: 不足知識=422 `knowledge_gap`、正本不一致=503 degraded、禁止語=設定に従い拒否/伏字。**黙って握りつぶさない**
- **レビュー**: 知識ベースの意味を変える解釈をコードに入れていないか、を必ず観点に含める

## 第12条 将来像

- **競走馬分析がメイン。人の鑑定はサブ機能**
- 現在は馬×騎手×レース日の3者分析から始め、将来的には利用者自身の生年月日も分析対象へ加え、第1条に定めた**馬×騎手×レース日×利用者の4者分析**を完成させる
- 拡張は本憲章の範囲内で行う(知識は正本へ、コードはGitHubへ、仮説はステータス付きで)

## 第13条 開発姿勢

- **完成より継続。**
- **速度より品質。**
- 一時的な実装より、長期運営できる設計を優先する
- 分からないことを分かったことにしない。未確定は未確定と書く

---

## このプロジェクトで最も大切にする10の原則

1. **当てるために作り、当たったことを一緒に喜ぶ。** ただし保証はしない。外れも同じ形式で開示する
2. **知識の正本はKomaVault、コードの正本はGitHub。** この分離を崩さない(2026-08-02にDriveから移設。分離そのものは不変)
3. **知識をコードに書かない。** エンジンは正本を読み込んで構築し、正本の差し替えだけで挙動が変わる設計を守る
4. **正本は検証してから使う。** ハッシュ不一致は止まるか、degradedを宣言する。黙ってフォールバックしない
5. **AIは占術家ではない。** 計算は知識ベースが決定し、AIはその結果を利用者が楽しめる「答え」として言葉にする
6. **仮説を事実として扱わない。** 未検証は `validation_required` / `app_hypothesis` を必ず明示する
7. **不足は補完せず、提案する。** 知識の欠落・矛盾は正本側への修正提案として報告する。知識は量より品質
8. **取得元に縛られない。** JRA-VANを含むあらゆるデータ源はDataProvider層で交換可能に保つ
9. **断定表現を禁止し、免責を必ず付す。** 買い目まで示すが、賭け金は指示しない。最後の判断は利用者のものとして残す
10. **完成より継続、速度より品質。** 過剰設計を避け、保守できる最小の構造を選ぶ

---

## 最終判断基準

AI・開発者が実装や設計で迷った場合は、

> **「短期的に楽な実装」ではなく「5年後も保守できる実装」を選択する。**

それでも迷う場合は、次の順に問うこと。

1. それは知識ベース(正本)に書くべきことか、コードに書くべきことか?
2. それは検証された事実か、仮説か? ステータスを明示したか?
3. 取得元・AI・特定技術に固定依存していないか?
4. 5年後の自分(または別のAI)が、この判断の理由を本書と正本から再現できるか?

この4問に答えられない変更は、行わずに提案として報告する。
本書の思想そのものに関わる場合は、第0条に従いプロジェクトオーナーの判断を仰ぐ。
